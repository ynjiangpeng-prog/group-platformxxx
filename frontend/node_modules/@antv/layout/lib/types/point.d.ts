type Point = [number, number] | [number, number, number];
type STDPoint = [number, number, number];
type PointObject = {
    x: number;
    y: number;
    z?: number;
};

export type { Point, PointObject, STDPoint };
